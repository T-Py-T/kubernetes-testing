apiVersion: v1
kind: Service
metadata:
  name: {{ include "ignition-namespace.fullname" . }}
  labels:
    {{- include "ignition-namespace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ignition-namespace.selectorLabels" . | nindent 4 }}
