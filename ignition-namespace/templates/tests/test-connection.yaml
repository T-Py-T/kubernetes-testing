apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ignition-namespace.fullname" . }}-test-connection"
  labels:
    {{- include "ignition-namespace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ignition-namespace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
